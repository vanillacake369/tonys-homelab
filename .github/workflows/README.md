# Disabled workflows

GitHub Actions are not the execution path for this repository.

The previous workflows are preserved under `.github/workflows.disabled/` as an
inventory of the replaced checks. Use `nix develop -c just ci`, `just cd-plan`,
and `just cd` for local or homelab-internal execution.
