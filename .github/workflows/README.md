# Disabled workflows

GitHub Actions are not the execution path for this repository.

The previous workflows are preserved under `.github/workflows.disabled/` as an
inventory of the replaced checks. Use `nix develop -c just check`, `just plan`,
and `just apply` for local or homelab-internal execution.
